# ── Required ──────────────────────────────────────────────────────────────────
# Get your free Groq key at: https://console.groq.com
GROQ_API_KEY = "gsk_your_groq_api_key_here"

# ── Optional: only needed for videos that have NO YouTube captions ────────────
# Without this, caption-free videos will fail on Streamlit Cloud because
# YouTube blocks audio downloads from datacenter IPs.
#
# How to get your cookies:
#   1. Install "Get cookies.txt LOCALLY" extension in Chrome/Edge
#   2. Go to youtube.com while logged in to your Google account
#   3. Click the extension → "Export cookies for this tab"
#   4. Paste the entire file contents below (keep the triple quotes)
#
# Note: cookies expire every few weeks — re-export if the error returns.
#
# YOUTUBE_COOKIES = """
# # Netscape HTTP Cookie File
# .youtube.com	TRUE	/	FALSE	...paste your cookies here...
# """
